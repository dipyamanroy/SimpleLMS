import { Card } from "@/components/ui/card"

interface DataCardProps {
    value: number,
    label: string,
    shouldFormat?: boolean,
}

export const DataCard = ({
    value,
    label,
    shouldFormat,
}: DataCardProps) => {
    return (
        <Card>
        </Card>
    )
}